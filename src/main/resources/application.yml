server:
  servlet:
    context-path: /
    session:
      timeout: 60
    #  cookie:
    #    name:

spring:
  config:
    activate:
      on-profile: prod
  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://127.0.0.1:3306/SAFT?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
#    username: saftmanager
#    password: 1234
---
spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        globally_quoted_identifiers: true
    show-sql: true


# Slf4j ???? > TRACE > DEBUG > INFO > WARN > ERROR > FATAL
logging:
  level:
    com.darts.saft: DEBUG
#    org.hibernate:
#      SQL: DEBUG
#      type: TRACE
#      type.BasicTypeRegistry: INFO
#    jdbc: WARN
